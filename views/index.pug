extends layout

block content
  div.container
  nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
    h4.white|Unit N_58_auth
    ul.navbar-nav.ml-auto
  .main.container
    .row
      .col-md-12.col-md-offset-3
        br
        h2.display-4.m-b-2 Auth
        div.response
        form.save(method='POST' name='formAuth' enctype="multipart/form-data")
          hr
          div.form-group
            label(for="inputEmail") Email address
            input(type="email" name="email" aria-describedby="emailHelp" placeholder="Enter email").form-control#inputEmail
            small#emailHelp.form-text.text-muted Correct email is: breaking_Bad@aol.com
          div.form-group
            label(for="inputPassword") Password
            input(type="password" name="password" aria-describedby="passwordHelp" placeholder="Password").form-control#inputPassword
            small#passwordHelp.form-text.text-muted Correct password is: AaronPaul
          div.form-group
          button.btn.btn-primary.btnAuth(type='submit') Авторизироваться
          br
          .tokenStatus
        hr
        button.btn.btn-primary.btnSend.disabled(type='submit') Отправить данные на [localhost:4000]
        .status